#include "main.h"
/**
 * _abs - computes the abs value of an integer
 * @t: is the integer to be computed
 * Return: the abs value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
